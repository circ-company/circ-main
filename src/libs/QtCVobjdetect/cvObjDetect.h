#pragma once

#include <QObject>

class cvObjDetect : public QObject
{
    Q_OBJECT
public: // types

public: // ctors
    explicit cvObjDetect(QObject *parent = nullptr);

public: // const

public: // non-const

    // ------------------------ properties ------------------------
private:
};
